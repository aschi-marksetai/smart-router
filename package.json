{
  "name": "smart-router",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "smart-router": "src/cli.ts"
  },
  "scripts": {
    "eval": "promptfoo eval -c evals/promptfooconfig.yaml",
    "eval:view": "promptfoo view",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun scripts/build.ts",
    "formula": "bun run scripts/formula.ts",
    "prettier-format": "prettier . --write"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "@earendil-works/pi-ai": "^0.85.1",
    "@typesafe-ai/sdk": "^0.6.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "prettier": "^3.8.2",
    "promptfoo": "^0.123.1",
    "typescript": "^7.0.2"
  }
}
